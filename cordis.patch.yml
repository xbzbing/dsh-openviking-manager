# dsh-openviking-manager provides a configuration UI, local manager, and a
# per-session toggle over the official OpenViking memory plugin (inject / strip
# / MCP tool gating). It does not replace or configure the official plugin itself.
- insert:
    - id: openviking-manager
      name: 'dsh-openviking-manager'
      config: {}
